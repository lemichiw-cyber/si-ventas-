services:
  # Tienda completa servida por Node dentro del contenedor
  - type: web
    name: dulce-encanto
    runtime: docker
    plan: free
    healthCheckPath: /api/config
    envVars:
      - key: JWT_SECRETO
        generateValue: true
